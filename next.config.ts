import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  outputFileTracingRoot: "C:\\Users\\ADMIN\\Desktop\\Gigil Frontend",
  images: {
    domains: ['localhost'],
    unoptimized: true
  }
};

export default nextConfig;
